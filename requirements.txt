# Full tested environment (Python 3.12). After installing these, install the project itself:  python -m pip install -e .
# (the tests and app.py import the qtraffic package from src/, and one test starts a fresh interpreter that needs it installed).
qiskit>=2.5.2,<3
qiskit-aer>=0.17.2,<0.18
numpy>=2.5.3,<3
scipy>=1.18.1,<2
networkx>=3.6.1,<4
matplotlib>=3.11.2,<4
pandas>=3.0.6,<4
plotly>=7.1.0,<8
streamlit>=1.64.0,<2
pytest>=9.1.1,<10
openai>=1.40,<3
